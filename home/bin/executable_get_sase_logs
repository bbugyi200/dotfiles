#!/bin/bash

if [[ $# -ne 1 ]]; then
  echo 1>&2 "usage: $(basename "$0") <LOG_PACK_BASE_DIR>"
  exit 2
fi

LOG_PACK_BASE_DIR="$1"
shift

scp -r bbc:~/.sase/logs/pack/"${LOG_PACK_BASE_DIR}" ~/tmp/
scp -r ~/tmp/"${LOG_PACK_BASE_DIR}"/ home:~/tmp/
