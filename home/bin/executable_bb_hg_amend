#!/bin/bash

set -e

should_upload=true
if [[ "$1" == "-U" || "$1" == "--no-upload" ]]; then
  should_upload=false
  shift
fi

set -x
hg update $(branch_name)
hg addremove
hg amend -n "$@"
set +x

if $should_upload; then
  set -x
  bb_hg_upload
  set +x
fi
