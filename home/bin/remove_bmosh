#!/bin/bash

MOSH_PREDICTION_DISPLAY=never mosh --predict=never --ssh="ssh -X -Y" "$@" -- \
  env TERM=xterm-256color COLORTERM=truecolor \
  LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 \
  NCURSES_NO_UTF8_ACS=1 \
  zsh
