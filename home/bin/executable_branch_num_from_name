#!/bin/bash

if [[ -z "$1" ]]; then
  echo "Usage: $0 <cl_name>"
  exit 1
fi

cl_name="$1"
shift
hg xl | grep "$cl_name" | grep "http://" | perl -nE "\$_ =~ s{.*/cl/(\d+).*}{\$1}g; print \$_ if /\$_/"
