local describe = require("busted").describe
local pending = require("busted").pending

describe("Unit test that", function()
	pending("flexes util.delete_file()")
	pending("flexes util.copy_to_clipboard()")
end)
