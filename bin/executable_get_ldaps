#!/bin/bash

perl -nE "print s{[ ,.?!;:()']}{ }gr" $(find ~/org -name '*.ho') \
  | perl -nE 'print s{[^ ]*[^ @,.?!;:)]\s}{}gr if /[a-z]+@/' \
  | perl -nE 'print s{[^a-z ][^ ]*@\s}{}gr' \
  | tr ' ' '\n' \
  | grep -E '@$' \
  | sort -u
